    - name: Install all dependencies required by Simple Web Application
      apt: name="{{ item }}" state=present
      with_items:
      - python 
      - python-setuptools 
      - python-dev 
      - build-essential
      - python-pip
      - python-mysqldb